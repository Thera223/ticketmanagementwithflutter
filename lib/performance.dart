import 'package:flutter/material.dart';

class PerformanceTrackingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Intégrer des graphiques ici en utilisant par exemple fl_chart
    return Center(
      child: Text("Visualisation des performances ici"),
    );
  }
}
